<?php

    require_once __DIR__ . '/config.php';
    class API {
        function select()
        $db = new Connect;
        $users = array();
        $data = $db->prepare("SELECT * FROM users ORDER BY")

    }